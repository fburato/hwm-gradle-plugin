prefix: "com.example."
modules:
    Main = "main.*"
    Endpoints = "endpoints.*"
    Core = "core.*"
    IO = "io.*"
    Config = "config.*"
rules:
    Main -> Endpoints -> Core
    Main -> IO -> Core
    Main -> Config
    Main -> Core